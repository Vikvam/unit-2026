package cz.aaa.unit2026

const val SERVER_HOST = "opentrack.basta.one"
const val SERVER_PORT = 443
const val SERVER_SECURE = true

/** Port the Ktor server binds to (behind Traefik, not exposed directly). */
const val SERVER_LISTEN_PORT = 8080